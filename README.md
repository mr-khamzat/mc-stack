# MeshCentral Stack

Полноценный стек мониторинга сети на базе MeshCentral.
Разворачивается на **любом Ubuntu сервере** за 5 минут.

## Что входит

| Компонент | Описание |
|-----------|----------|
| 🤖 **Telegram Bot** | Мониторинг агентов MC, инвентарь, алерты, WiFi зонды, удалённые команды |
| 🗄 **RackViz** | Визуализатор серверной стойки с патчкордами и drag-and-drop |
| 🗺 **NetMap** | Интерактивная карта сети по подсетям |
| 📡 **WiFi зонды** | Сканирование клиентов локальной сети через агентов MC |

## Скриншоты

> RackViz — визуализация стойки с патчкордами
> NetMap — интерактивная карта устройств
> Бот — мониторинг, инвентарь, алерты

---

## Быстрый старт

### 1. Требования
- **Ubuntu 22.04 или 24.04** (рекомендуется VM в Proxmox)
- Минимум: 2 CPU, 2 GB RAM, 20 GB диск
- Интернет для первичной установки пакетов
- Работающий сервер **MeshCentral** (локальный или удалённый)

### 2. Установка

```bash
git clone https://github.com/mr-khamzat/mc-stack.git
cd mc-stack
sudo bash deploy.sh
```

Скрипт задаст вопросы и всё настроит. **Ничего редактировать вручную не нужно.**

### 3. Что спросит скрипт

| Вопрос | Где взять |
|--------|-----------|
| Telegram Bot Token | @BotFather → `/newbot` |
| Твой Chat ID | @userinfobot в Telegram |
| MeshCentral URL | URL твоего MC сервера |
| MeshCentral WSS | Обычно `wss://твой-mc:443` |
| MC логин/пароль | Учётные данные администратора MC |
| MC Token Key | На MC сервере: `node meshcentral.js --logintokenkey` |
| RackViz пароль | Придумай сам |
| IP/домен сервера | IP нового сервера |
| HTTP порт | По умолчанию 8080 |

---

## После установки

### Доступ
- **RackViz**: `http://IP:8080/rack/`
- **NetMap**: `http://IP:8080/netmap` (войди через RackViz)
- **Бот**: отправь `/start` своему боту в Telegram

### Управление сервисами

```bash
# Бот
systemctl status meshcentral-bot
systemctl restart meshcentral-bot
journalctl -u meshcentral-bot -f

# RackViz
cd /opt/rackviz
docker compose ps
docker compose logs -f
docker compose restart
```

---

## Структура репозитория

```
mc-stack/
├── deploy.sh              # 🚀 Главный скрипт установки
├── pack.sh                # 📦 Упаковать для переноса (tar.gz)
├── README.md              # 📖 Эта инструкция
├── .gitignore
│
├── bot/
│   ├── bot.py             # Telegram бот (6500+ строк)
│   ├── keenetic_probe.ps1 # PowerShell зонд WiFi-клиентов
│   ├── device_probe.ps1   # PowerShell зонд устройств
│   ├── vis-network.min.js # JS библиотека для NetMap
│   └── .env.example       # Пример конфига (скопируй в .env)
│
├── rackviz/
│   ├── docker-compose.yml # Docker конфигурация
│   ├── .env.example       # Пример конфига RackViz
│   ├── backend/           # FastAPI + SQLite
│   │   ├── Dockerfile
│   │   ├── requirements.txt
│   │   └── app/
│   │       ├── main.py
│   │       ├── models.py
│   │       ├── auth.py
│   │       ├── database.py
│   │       ├── seed.py
│   │       └── routers/
│   └── frontend/          # React + TypeScript + Vite
│       ├── Dockerfile
│       ├── package.json
│       └── src/
│
└── nginx/
    └── meshcentral-stack.conf.template  # nginx конфиг
```

---

## Telegram бот — возможности

### Мониторинг
- 🖥 Статус MeshCentral (онлайн/офлайн, количество устройств)
- 🟢/⚪ Список всех устройств с онлайн-статусом
- 🔔 Алерты: диск >80%, антивирус, устройство офлайн >N часов, новое устройство
- 📈 Графики аптайма устройств

### Инвентарь
- 📦 Полный инвентарь железа (CPU, RAM, диск, ОС, версии)
- 💿 Инвентаризация установленного ПО
- ⚖️ Сравнение двух устройств
- 📊 Экспорт в Excel / PDF

### Управление
- 🖥 Удалённые команды (PowerShell/Bash) на любой агент
- 📁 Команды на группу агентов одновременно
- 📝 Сохранённые скрипты (библиотека быстрых команд)
- 🔄 Перезагрузка / выключение устройств
- 📡 Wake-on-LAN

### WiFi / Сеть
- 📡 Зонды Keenetic — сканирование WiFi клиентов по офисам
- 🗺 Интерактивная карта сети (NetMap) с фильтрами по подсетям
- 🖨 Инвентарь принтеров

### Администрирование
- 💾 Бэкап конфига MeshCentral в Telegram
- 🗄 Полный бэкап сервера (zip с паролем)
- 🆕 Проверка + обновление MeshCentral
- 🔇 Mute-режим (техобслуживание без алертов)
- 🚀 Инструкция по развёртыванию копии

---

## RackViz — возможности

- 🗄 Визуализация серверной стойки в SVG
- 🔌 Назначение портов: MeshCentral агент / ручное / WiFi-сосед
- 🔗 Патчкорды — цветные кабели между портами
- ↕ Перемещение оборудования (drag-and-drop + ▲▼)
- 🗺 Авторизация единым входом с NetMap
- 👁 Просмотр без входа, редактирование только для admin

---

## Работа без интернета (локальная сеть)

После первичной установки (которая требует интернет для Docker и npm) **стек полностью работает без интернета**:

- Бот → локальный сервер MeshCentral (по LAN)
- RackViz → Docker образы уже скачаны
- NetMap → генерируется ботом локально

Для Proxmox VM: выставь IP из твоей локальной подсети, укажи этот IP при установке.

---

## Обновление

```bash
git pull
# Бот:
systemctl restart meshcentral-bot
# RackViz:
cd /opt/rackviz && docker compose build --no-cache && docker compose up -d
```

---

## Перенос на другой сервер

### Вариант 1: через GitHub (рекомендуется)
```bash
git clone https://github.com/mr-khamzat/mc-stack.git
cd mc-stack && sudo bash deploy.sh
```

### Вариант 2: через архив (без интернета)
```bash
# На старом сервере:
bash /opt/deploy-kit/pack.sh
scp /tmp/mc-stack-deploy-*.tar.gz root@НОВЫЙ_IP:/tmp/

# На новом сервере:
cd /tmp && tar xzf mc-stack-deploy-*.tar.gz
cd mc-stack-deploy/deploy-kit && bash deploy.sh
```

Или через кнопку в боте: **🔧 Инструменты → 🚀 Развернуть копию → 📦 Упаковать с этого сервера**

---

## FAQ

**Q: Нужен ли публичный IP?**
A: Нет. Бот работает через Telegram (исходящий HTTPS). RackViz и NetMap — только в локальной сети.

**Q: Нужен ли домен и SSL?**
A: Нет. Для локальной сети достаточно HTTP на любом порту.

**Q: MeshCentral нужно устанавливать отдельно?**
A: Да. Этот стек подключается к существующему MC серверу. Инструкция по установке MC: [meshcentral.com/info](https://meshcentral.com/info)

**Q: Можно ли несколько ботов к одному MC серверу?**
A: Да. Каждый бот = свой токен, свой Chat ID. Один MC сервер обслуживает всех.

---

## Лицензия

MIT — используй как хочешь.
